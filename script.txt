#!/bin/bash

while [[ true ]] ; do

	# performs synchronizations / copy
	rclone move /root/downloads/ remotedriveNAMEhere:/ --exclude *.aria2 -P

	# waiting for something to change or it will pass 300 seconds
	inotifywait --recursive --timeout 300 -e modify,delete,create,move   /root/downloads/

	# going back to the beginning
done
